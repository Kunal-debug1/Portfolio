import { notFound } from "next/navigation"; import Link from "next/link"; import { ArrowLeft, ArrowUpRight, Code2 } from "lucide-react"; import { projects } from "@/data/portfolio";
export function generateStaticParams() { return projects.map(({ slug }) => ({ slug })); }
export default async function ProjectPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const project = projects.find(p => p.slug === slug); if (!project) notFound(); return <main className="case-study shell"><Link href="/#work" className="back-link"><ArrowLeft size={17} /> Selected work</Link><div className="case-hero"><div><p className="section-label">{project.category} / {project.year}</p><h1>{project.title}</h1><p>{project.description}</p></div><div className="case-symbol">{project.symbol}</div></div><div className="case-links"><a className="button button-primary" href={project.github} target="_blank" rel="noreferrer"><Code2 size={17} /> Source on GitHub</a>{project.live && <a className="button button-quiet" href={project.live} target="_blank" rel="noreferrer">Live project <ArrowUpRight size={17} /></a>}</div><div className="case-grid"><article><span className="section-label">THE PROBLEM</span><p>{project.problem}</p></article><article><span className="section-label">THE SOLUTION</span><p>{project.solution}</p></article><article><span className="section-label">ENGINEERING</span><p>{project.engineering}</p></article><article><span className="section-label">CORE FEATURES</span><ul>{project.features.map(f => <li key={f}>{f}</li>)}</ul></article></div><div className="case-stack"><span className="section-label">TECHNOLOGY</span><div className="tags">{project.technologies.map(t => <span key={t}>{t}</span>)}</div></div></main> }

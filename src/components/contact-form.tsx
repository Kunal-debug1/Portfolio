"use client";
import { FormEvent, useState } from "react"; import { ArrowUpRight } from "lucide-react";
export function ContactForm({ email }: { email: string }) { const [sent, setSent] = useState(false); function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const data = new FormData(e.currentTarget); const subject = encodeURIComponent(`${data.get("subject")} — from ${data.get("name")}`); const body = encodeURIComponent(`${data.get("message")}\n\nReply to: ${data.get("email")}`); window.location.href = `mailto:${email}?subject=${subject}&body=${body}`; setSent(true); } return <form className="contact-form" onSubmit={submit}><label>Name<input name="name" required placeholder="Your name" /></label><label>Email<input type="email" name="email" required placeholder="you@company.com" /></label><label>Subject<input name="subject" required placeholder="What can I help with?" /></label><label>Message<textarea name="message" required rows={4} placeholder="A little about the project or role." /></label><button className="button button-primary" type="submit">Start a conversation <ArrowUpRight size={17} /></button>{sent && <p className="form-note">Your mail client is ready—send the drafted message to continue.</p>}</form> }

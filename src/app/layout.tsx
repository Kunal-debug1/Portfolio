import type { Metadata } from "next"; import { Geist, Geist_Mono } from "next/font/google"; import "./globals.css";
const geist = Geist({ variable: "--font-geist", subsets: ["latin"] }); const mono = Geist_Mono({ variable: "--font-mono", subsets: ["latin"] });
export const metadata: Metadata = { title: { default: "Kunal Gaikwad — Software Engineer", template: "%s | Kunal Gaikwad" }, description: "Software Engineer and Python backend developer building reliable APIs, data-driven web applications, and intelligent systems.", metadataBase: new URL("https://kunal-debug1.github.io/Portfolio/"), alternates: { canonical: "/" }, openGraph: { type: "website", title: "Kunal Gaikwad — Software Engineer", description: "Reliable APIs, data-driven applications, and intelligent systems." }, twitter: { card: "summary", title: "Kunal Gaikwad — Software Engineer", description: "Reliable APIs, data-driven applications, and intelligent systems." } };
const themeScript = `(() => { try { const saved = localStorage.getItem("theme"); const dark = saved ? saved === "dark" : matchMedia("(prefers-color-scheme: dark)").matches; document.documentElement.dataset.theme = dark ? "dark" : "light"; } catch {} })();`;
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) { return <html lang="en" suppressHydrationWarning className={`${geist.variable} ${mono.variable}`}><head><script dangerouslySetInnerHTML={{ __html: themeScript }} /></head><body>{children}</body></html>; }
